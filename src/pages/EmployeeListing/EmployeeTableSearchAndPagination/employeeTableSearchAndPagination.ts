import styled from "styled-components";
import colors from "../../../core/constants/colors";

const EmployeeTableSearchAndPaginationWrapper = styled.div`
.border{
    border-radius: 10px;
    border: 1px solid ${colors.LIGHT_GRAY_COLOR};
}
`;
export default EmployeeTableSearchAndPaginationWrapper;
