export enum SortDirection {
    ASC = "asc",
    DESC = "desc",
    NO_SORT = "no-sort",
}

export const apiURL = {
    employee: "/employee",
    skills: "/skills",
    roles: "/roles",
    departments: "/departments",
    authSignIn: "/auth/sign-in"
  };