export interface ISkill {
    id: number;
    skill: string;
}

export interface IDepartment {
    id: number;
    department: string;
}

export interface IRole {
    id: number;
    role: string;
}

export interface IEmpMode {
    id: number;
    mode: string;
}