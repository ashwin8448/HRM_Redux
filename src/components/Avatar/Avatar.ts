import styled from "styled-components";

const AvatarWrapper = styled.div`

display: flex;
gap: 10px;
img{
    width: 100px;
    height: 100px;
    border-radius: 50%;
}
`;

export default AvatarWrapper;
